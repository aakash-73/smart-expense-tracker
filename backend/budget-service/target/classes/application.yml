server:
  port: 8085

spring:
  application:
    name: budget-service

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/smart-expense-tracker}
    auto-index-creation: true

  config:
    import: optional:file:../.env[.properties]

logging:
  level:
    root: INFO
    com.smarttracker: DEBUG
