package com.smarttracker.budgetservice.config;

// Ignite integration disabled — budget-service uses MongoDB only.
// Class kept as placeholder so git history is preserved.
public class IgniteConfig {
}
